---
import BaseLayout from '../layouts/BaseLayout.astro';
import { Hero } from '../components/Hero.tsx';
import { BancosSection } from '../components/BancosSection.tsx';
import { MissionVisionValues } from '../components/MissionVisionValues.tsx';
import { UnicaShorts } from '../components/UnicaShorts.tsx';
import { NossoSistema } from '../components/NossoSistema.tsx';
import { ProductsSection } from '../components/ProductsSection.tsx';
---

<BaseLayout
  title="A solução mais eficaz para você"
  description="Utilizamos nossa experiência e conhecimento diariamente para oferecer a solução mais eficaz para você."
>
  <!-- Hidratada logo (client:load): é o formulário de lead, o objetivo comercial
       principal do site — precisa estar interativo assim que a página carrega,
       sem esperar entrar na viewport (já está visível de cara). -->
  <Hero client:load />

  <!-- Hidratada: carrossel (Swiper) + modal de bancos com focus trap. -->
  <BancosSection client:visible />

  <!-- Sem client:*: zero JavaScript — mesmo componente estático do piloto da Sobre. -->
  <MissionVisionValues />

  <!-- Hidratada: clique pra abrir vídeo em modal + ESC pra fechar. -->
  <UnicaShorts client:visible />

  <!-- Sem client:*: grid de cards 100% estático, sem nenhuma interação/animação
       no componente original. -->
  <ProductsSection />

  <!-- Hidratada: cards com fade-in em cascata (Framer Motion). -->
  <NossoSistema client:visible />
</BaseLayout>
